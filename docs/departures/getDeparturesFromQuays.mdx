---
name: getDeparturesFromQuays
menu: Departures
route: /departures/getDeparturesFromQuays
---

# getDeparturesFromQuays

```typescript
(quayIds: Array<string>, params?: GetDeparturesParams) => Promise<Array<{ id: string, departures: Departure[] } | undefined>>
```

`getDeparturesFromQuays` finds departures from one or more given quays.

The method will return an array of objects containing fields for the quay's `id` and its corresponding `departures`.

## Parameters

### quayIds (`Array<string>`)
The IDs of the quays you are interested in.

### params (`Object`) [Optional]

See the `params` parameter for [`getDeparturesForStopPlace`](/departures/getDeparturesFromStopPlace#params-object-optional).
